{{ bundle_prefix|replace('_', '.') }}.{{ entity_pluralize|lower }}:
    type: rest
    resource: {{ namespace }}\Controller{{ entity_namespace ? '\\' ~ entity_namespace : '' }}\{{ entity_class }}Controller
    prefix: {{ bundle_namespace }}
    name_prefix: {{ bundle_prefix }}_
