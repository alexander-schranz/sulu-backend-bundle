        // no language redirect: {% include 'sulu/comment.twig' %}

        sandbox.mvc.routes.push({
            route: '{{ bundle_namespace }}/{{ entity_pluralize|snake_case('-') }}',
            callback: function () {
                var language = getContentLanguage();
                sandbox.emit('sulu.router.navigate', '{{ bundle_namespace }}/{{ entity_pluralize|snake_case('-') }}/' + language);
            }
        });

        // list all entities: {% include 'sulu/comment.twig' %}

        sandbox.mvc.routes.push({
            route: '{{ bundle_namespace }}/{{ entity_pluralize|snake_case('-') }}/:language',
            callback: function (language) {
                return '<div data-aura-component="{{ entity_pluralize|snake_case('-') }}/list@{{ js_bundle_name }}" data-aura-language="' + language + '"/>';
            }
        });

        // add entity: {% include 'sulu/comment.twig' %}

        sandbox.mvc.routes.push({
            route: '{{ bundle_namespace }}/{{ entity_pluralize|snake_case('-') }}/:language/add/:content',
            callback: function (language, content) {
                return '<div data-aura-component="{{ entity_pluralize|snake_case('-') }}/edit@{{ js_bundle_name }}" data-aura-language="' + language + '"/>';
            }
        });

        // edit entity: {% include 'sulu/comment.twig' %}

        sandbox.mvc.routes.push({
            route: '{{ bundle_namespace }}/{{ entity_pluralize|snake_case('-') }}/:language/edit::id/:content',
            callback: function (language, id, content) {
                return '<div data-aura-component="{{ entity_pluralize|snake_case('-') }}/edit@{{ js_bundle_name }}" data-aura-language="' + language + '" data-aura-id="' + id + '"/>';
            }
        });
