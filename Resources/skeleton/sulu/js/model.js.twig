/**
 * {% include 'sulu/comment.twig' %}
 */

define(['mvc/relationalmodel'], function (relationalModel) {

    'use strict';

    return relationalModel({
        urlRoot: '/admin/api/{{ entity|lower }}',

        defaults: function () {
            return {
{% for column in metadata.columnNames %}
                {{ column }}: null{% if not loop.last %},{% endif %}
{% endfor %}
            };
        }
    });
});
