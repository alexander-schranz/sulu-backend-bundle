services:
    {{ bundle_prefix }}.manager.{{ entity|snake_case }}:
        class: {{ namespace }}\Manager{{ entity_namespace ? '\\' ~ entity_namespace : '' }}\{{ entity_class }}Manager
        arguments:
            - '@doctrine.orm.entity_manager'
            - '@{{ bundle_prefix }}.repository.{{ entity|snake_case }}'
